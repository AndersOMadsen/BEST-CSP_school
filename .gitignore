# Word/Office lock files and personal notes
*.docx
*.xlsx
~$*

# Editor backup and swap files
*.py~
*~
*.swp
*.swo

# macOS metadata
.DS_Store
.AppleDouble

# Jupyter checkpoints
.ipynb_checkpoints/
__pycache__/

# Python environments
.venv/
venv/
*.pyc

# Local output from cod_checker (don't commit bulk downloads)
cod_checks/

# PLATON runtime and session files (binary / session-specific, not source)
platon.bin
platon.def
platon.hkp
platon.pjn
platon.sum
platon.
platon_pl.res

# Papers directory — copyrighted PDFs, not for version control
Papers/

# Large logo pack (24 MB) — use BEST-CSP_logos/ instead
COST_logo_newbranding_pack2021/

# Working trash and session files
trash/
claude.resume

# Loose working files in the root
/*.cif
/reciprocal_space*

# Pipeline — generated structure data (580 MB); track only source code
Pipeline/Good_Structures_and_data/
Pipeline/xylitol/
